@model Kolokwium.ViewModel.ViewModels.AddCheckInToClientVm

@{
    ViewData["Title"] = "AddCheckInToClient";
}

<h1>AddCheckInToClient</h1>

<h4>AddCheckInToClientVm</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddCheckInToClient">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ClientId"/>
            <div class="form-group">
                <label asp-for="CheckInId" class="control-label"></label>
                <input asp-for="CheckInId" class="form-control" />
                <span asp-validation-for="CheckInId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
